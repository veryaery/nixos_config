generate store paths for each theme
create script that creates symlinks

{
    ".xmonad/lib/Theme.hs" = themeExpr;
}
^ each theme-reactive file is already in here
create symlink for each of these files

remove all files in .prevtheme
remove parent directories if they are empty
create symlinks for all files in dotfiles store

create .prevtheme
add all file paths to .prevtheme